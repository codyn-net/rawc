_cpgrawc() {
	cur=`_get_cword`

	if [[ "$cur" != -* ]]; then
		_filedir '@(cpg|xml)'
	else
		COMPREPLY=($(compgen -W "$(cpgrawc --list-options)" -- "$cur"))
	fi

	return 0
}

complete -F _cpgrawc cpgrawc
